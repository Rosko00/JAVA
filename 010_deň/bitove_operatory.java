package 010_deň;

public class bitove_operatory {
    public static void main(String[] args) {
        // (~) = Negovanie bitov (NOT)
        // (&) = Bitobé (AND)
        // (|) = Bitové (OR)
        // (^) = Exkluzívne bitové OR (XOR)
        // (>>) = Posun doprava 
        // (>>>) = Posun doprava bez znamienka
        // (<<) = Posun doľava
        // (&=) = Bitové (AND) s priradením
        // (!=) = Bitové (OR) s priradením
        // (^=) = Exkluzívne bitové OR s priradením
        // (>>=) = Posun doprava s priradením
        // (>>>=) = Posun doprava s priradením bez znamienka
        // (<<=) = Posun doľava s priradením

    }
    
}
